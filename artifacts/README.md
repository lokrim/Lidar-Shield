# Experiment artifacts

Generated work belongs under `artifacts/<experiment_id>/` and is ignored by
Git. Each experiment is isolated and immutable after completion; reruns use a
new experiment ID. See [`docs/MANIFESTS.md`](../docs/MANIFESTS.md) for the
required layout and provenance fields.

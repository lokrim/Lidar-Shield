# Reports

Reproducible report products will be written to `figures/`, `tables/`, and
`model_cards/` by later milestone commands. M0 creates only these documented,
ignored output locations; it does not implement a report generator.
